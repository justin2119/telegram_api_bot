name: telegram_api_bot
description: A Telegram bot that provides weather information using teledart and OpenWeatherMap API.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  teledart: ^0.2.14
  http: ^1.1.0
  dotenv: ^3.1.2
